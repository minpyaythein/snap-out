## 1. Popup HTML

- [x] 1.1 Add an "Apply" button inside the duration section in `popup.html`

## 2. Popup CSS

- [x] 2.1 Add styles for the Apply button in `popup.css`

## 3. Popup JS

- [x] 3.1 Remove the `change` event listeners from `durationMin` and `durationSec` in `popup.js`
- [x] 3.2 Add a `click` event listener on the Apply button that calls `saveDuration()` in `popup.js`
